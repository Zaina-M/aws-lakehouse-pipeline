# Project-specific variables for local development.
# AWS credentials are NOT stored here — run `aws configure` instead.
# Copy to .env and fill in SCRIPTS_BUCKET after the first terraform apply.

ENV=dev
SCRIPTS_BUCKET=lakehouse-dev-scripts-XXXXXXXXXXXX
